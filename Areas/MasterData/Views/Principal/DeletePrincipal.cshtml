@using NoiSys.Areas.MasterData.ViewModels;

@{
    ViewBag.Title = "Hapus Principal";
    Layout = "~/Views/Shared/_TopNavbarMasterData.cshtml";
}

@model DetailPrincipalViewModel

<div class="card card-red">
    <div class="card-header">
        <h3 class="card-title" style="color:#ffffff">@ViewBag.Title</h3>
    </div>


    <form enctype="multipart/form-data" asp-controller="Principal" asp-action="DeletePrincipal" method="post" class="form-horizontal">
        <div class="card-body">
            <h4>Yakin hapus data ini?</h4>
            <hr />
            <input hidden asp-for="PrincipalId" />
            <input hidden asp-for="KodePrincipal" />
            <input hidden asp-for="NamaPrincipal" />
            <div class="post">
                <div class="user-block">                    
                    <span class="username">
                        <a href="#">@Html.DisplayFor(model => model.NamaPrincipal)</a>
                    </span>
                    <span class="description">@Html.DisplayFor(model => model.KodePrincipal)</span>
                </div>
                <!-- /.user-block -->
            </div>
        </div>

        @if (TempData["WarningMessage"] != null)
        {
            <script type="text/javascript">
                window.onload = function () {
                    swal("Gagal", '@TempData["WarningMessage"]', "warning");
                };
            </script>
        }

        <div class="card-footer">
            @*<form asp-action="DeletePrincipal" method="post">*@
            <a class="btn btn-default" asp-action="Index" asp-controller="Principal" asp-area="MasterData">Batal</a>
            <button type="submit" class="btn btn-danger float-left fw-bold"><div style="color:#ffffff">Hapus</div></button>
            @*</form>*@
        </div>
    </form>
</div>
